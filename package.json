{
  "name": "react-apollo-pagination",
  "version": "1.0.11",
  "description": "React hook for pagination in Apollo-Client",
  "main": "lib/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "babel src --out-dir lib",
    "prepare": "npm run build",
    "prepublish": "npm run build",
    "postpublish": "git push origin --all; git push origin --tags",
    "postversion": "npm publish",
    "preversion0": "npm test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PavelPoroskov/react-apollo-pagination.git"
  },
  "keywords": [
    "pagination",
    "apollo-client",
    "graphql"
  ],
  "author": "Pavel Poroskov",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PavelPoroskov/react-apollo-pagination/issues"
  },
  "homepage": "https://github.com/PavelPoroskov/react-apollo-pagination#readme",
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.4",
    "@babel/node": "^7.5.0",
    "@babel/preset-env": "^7.5.4",
    "@babel/preset-react": "^7.0.0"
  },
  "peerDependencies": {
    "react": "^16.8.6"
  }
}
